export interface Props {
  visible: boolean;
  height: string;
  width: string;
  color: string;
  strokeWidth: string;
  animationDuration: string;
  ariaLabel: string;
  wrapperStyle: React.CSSProperties;
  wrapperClass: string;
}
