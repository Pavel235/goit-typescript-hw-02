export interface LoaderProps {
  visible: boolean;
  height: string;
  width: string;
  color: string;
  strokeWidth: string;
  animationDuration: string;
  ariaLabel: string;
  wrapperStyle: React.CSSProperties;
  wrapperClass: string;
}
